# Exercise 14: Security and Image Optimization

# 1. Implemented a non-root user (nginx) to run the application.
# 2. Adjusted permissions for Nginx system folders (/var/cache/nginx, /var/run/nginx.pid) 
#    to allow the unprivileged user to write logs and PID files.
# 3. Created a .dockerignore file to reduce build context size.

# Command to build:
$ docker build --build-arg VITE_BACKEND_URL="http://localhost:3000/api" -t todo-frontend -f todo-app/todo-frontend/Dockerfile todo-app/todo-frontend/

# Verification of non-root execution (from container logs):
# nginx: [warn] the "user" directive makes sense only if the master process runs with super-user privileges, ignored in /etc/nginx/nginx.conf:2